- content_for :header do
  = t("views.profiles.edit.profile_disabled.header")

- content_for :details do
  = t("views.profiles.edit.profile_disabled.details")

- content_for :instructions do
  = t("views.profiles.edit.profile_disabled.instructions")
