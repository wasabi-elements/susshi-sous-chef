- content_for :header do
  = t("views.sessions.authorize.api.header")

- content_for :details do
  = t("views.sessions.authorize.api.details")

- content_for :instructions do
  = t("views.sessions.authorize.api.instructions.message")
  %br
  %br
  = t("views.sessions.authorize.api.instructions.try_again")
