- content_for :header do
  = t("views.sessions.authorize.gone.header")

- content_for :details do
  = t("views.sessions.authorize.gone.details")

- content_for :instructions do
  = t("views.sessions.authorize.gone.instructions.message")
  %br
  %br
  = t("views.sessions.authorize.gone.instructions.try_again")
