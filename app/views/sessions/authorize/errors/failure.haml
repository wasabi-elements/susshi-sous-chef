- content_for :header do
  = t("views.sessions.authorize.failure.header")

- content_for :details do
  = t("views.sessions.authorize.failure.details")

- content_for :instructions do
  = t("views.sessions.authorize.failure.instructions.message")
  %br
  %br
  = t("views.sessions.authorize.failure.instructions.try_again")
