- content_for :header do
  = t("views.sessions.authorize.login.header")

- content_for :details do
  = t("views.sessions.authorize.login.details")

- content_for :instructions do
  = t("views.sessions.authorize.login.instructions.message")
  %br
  %br
  = t("views.sessions.authorize.login.instructions.try_again")
